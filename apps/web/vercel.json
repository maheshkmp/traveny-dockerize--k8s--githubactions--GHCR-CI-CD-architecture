{
  "bunVersion": "1.x",
  "installCommand": "bun install && bun run --filter core build && bun run --filter api build:types",
  "buildCommand": "bun run build",
  "crons": [
    {
      "path": "/api/cron/refresh-tiktok",
      "schedule": "0 2 * * *"
    }
  ]
}
